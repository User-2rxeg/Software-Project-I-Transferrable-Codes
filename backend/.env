
AUDIT_TTL_DAYS=180

MONGODB_URI=mongodb://127.0.0.1:27017/Softwarecodes

PORT=3786

NODE_ENV=development

JWT_SECRET=yourSuperSecret

JWT_EXPIRES_IN=1h


EMAIL_USER=eyadelmaleh07@gmail.com
EMAIL_APP_PASSWORD=waqf oelw papk khra

FEEDBACK_TO_EMAIL=eyadelmaleh07@gmail.com

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false



#MONGODUMP_PATH=mongodump                    # optional if mongodump in PATH
#BACKUP_LOCAL_DIR=./backups                  # optional
#BACKUP_CRON=*/5 * * * *                    # optional: CRON expression; default daily 02:00 UTC
#BACKUP_TIMEZONE=0 17 * * *                        # optional timezone for cron
#BACKUP_USE_OPLOG=true|false                 # optional default for scheduled backups (string 'true' to enable)
#BACKUP_DUMP_USERS=true|false                # optional: include DB users/roles when dumping a single DB
#BACKUP_NAME_PREFIX=scheduled                # optional prefix for scheduled backup filenames


# Path where backups are stored (relative to project root or absolute)
BACKUP_LOCAL_DIR=./backups

# Test schedule: every 5 minutes (enable with BACKUP_ENABLE_TEST=true)
BACKUP_ENABLE_TEST=true


# Production schedule: daily at 17:00 (5 PM) Egypt time
#BACKUP_CRON_PROD=0 17 * * *
BACKUP_CRON_TEST=*/1 * * * *    # runs every minute for quick testing (then revert)
BACKUP_TIMEZONE=Africa/Cairo

# Optional flags for scheduled jobs (strings 'true' to enable)
BACKUP_USE_OPLOG=false
BACKUP_DUMP_USERS=false

# Path to mongodump if not in PATH (optional)
MONGODUMP_PATH=mongodump




